# Claude Configuration Instructions

This directory contains governed Claude Code configuration artifacts only. Do not place application source here.

Before changing an artifact, define the transaction write set and run the required deterministic validation. Generated policy under `control-plane/generated/` is read-only. Follow the nearest nested `AGENTS.md` if one exists; otherwise follow the repository-level instructions.
