---
paths:
  - ".claude/agents/*.md"
---

# Agent definitions

- Keep each agent's scope, tools, and ownership aligned with `manifest.yaml` and generated capabilities.
- Writers own only their listed paths; auditors and verifiers remain read-only and cannot authorize.
